{
  "name": "tourism-platform",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.18.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "postman:build": "node docs/postman/src/build.mjs",
    "postman:check": "node docs/postman/src/build.mjs --check",
    "postman:seed": "node docs/postman/seed-test-data.mjs",
    "postman:test": "pnpm dlx newman@6 run docs/postman/tourism-api.json -e .tmp/postman.env.json --reporters cli --reporter-cli-no-banner"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.3.0"
  }
}
